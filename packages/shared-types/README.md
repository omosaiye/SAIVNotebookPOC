# @private-llm/shared-types

Frozen TypeScript enum and contract definitions shared by frontend and backend integration work.

## Canonical enum source

Canonical enum values are frozen in `contracts/enums.json` and mirrored in `src/enums.ts`.

## Exported contracts

- `FileSummary`
- `FileDetail`
- `UploadResponse`
- `ChatQueryRequest`
- `ChatQueryResponse`
- `CitationResponse`
- `PendingUploadAndAskRequestState`
